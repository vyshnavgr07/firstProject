import React from 'react'
import './Body.Module.css'

const Body = () => {
  return (
    <div>
     <p>A component can be functional or a class that optionally takes input and returns an element.<br/>. The element does not have any methods. Each component has its life cycle methods.<br/> A React element is an object representation of a DOM node</p>
    
     
      

    </div>
  )
}

export default Body